import React,{useState, useEffect} from 'react';
import styles from './feedcard.module.scss';
import { v4 as uuid } from 'uuid';





const FeedCard = ({ toggle}) => {

    return (
        <div>
            Heelo !
        </div>
    );
};

export default FeedCard;